# 默认窗口无标题栏，16:9的窗口比例启动
window:
    decorations: "none"
    dimensions:
      columns: 105
      lines: 27

# 背景半透明，方便抄代码
background_opacity: 0.8

# 实时重载配置文件，不用重启测试配置选项
live_config_reload: true

# 字体
font:
  size: 10
  normal:
    family: OperatorMono Nerd Font
